import type { Product } from "@/lib/storefront/types";
const base = process.env.NEXT_PUBLIC_SITE_URL ?? "http://localhost:3000";
export const organizationJsonLd = { "@context": "https://schema.org", "@type": "Organization", name: "Pixels Galaxy", url: base, logo: `${base}/brand/pixels-galaxy-logo.png` };
export const websiteJsonLd = { "@context": "https://schema.org", "@type": "WebSite", name: "ZipString Pakistan by Pixels Galaxy", url: base, description: "ZipString ke saath screen-free family fun aur 100+ glowing tricks." };
export function buildProductJsonLd(product: Product) { return { "@context": "https://schema.org", "@type": "Product", name: product.name, image: product.media.map((x) => x.src), description: product.description, sku: product.id, brand: { "@type": "Brand", name: "ZipString" }, aggregateRating: { "@type": "AggregateRating", ratingValue: product.rating, reviewCount: product.reviews }, offers: { "@type": "Offer", url: `${base}/#featured`, priceCurrency: "PKR", price: (product.price / 100).toFixed(2), availability: "https://schema.org/InStock" } }; }
