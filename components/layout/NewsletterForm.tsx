"use client";
import { FormEvent, useState } from "react";
export function NewsletterForm() { const [message, setMessage] = useState(""); const submit = (e: FormEvent<HTMLFormElement>) => { e.preventDefault(); const form = new FormData(e.currentTarget); const email = String(form.get("email") ?? ""); setMessage(/^\S+@\S+\.\S+$/.test(email) ? "Saved for this demo — email delivery will be connected later." : "Enter a valid email address."); }; return <form className="newsletter" onSubmit={submit} noValidate><label htmlFor="email">STAY IN THE LOOP</label><div><input id="email" name="email" type="email" placeholder="Enter your email" aria-describedby="newsletter-message" /><button>JOIN THE GALAXY</button></div><p id="newsletter-message" aria-live="polite">{message || "Be first to know about new drops and challenges."}</p></form>; }
