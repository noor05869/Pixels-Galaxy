"use client";
import { FormEvent, useState } from "react";
export function NewsletterForm() { const [message, setMessage] = useState(""); const submit = (e: FormEvent<HTMLFormElement>) => { e.preventDefault(); const form = new FormData(e.currentTarget); const email = String(form.get("email") ?? ""); setMessage(/^\S+@\S+\.\S+$/.test(email) ? "Thanks — email updates will be connected soon." : "Please enter a valid email address."); }; return <form className="newsletter" onSubmit={submit} noValidate><label htmlFor="email">GET THE LATEST FIRST</label><div><input id="email" name="email" type="email" placeholder="Enter your email" aria-describedby="newsletter-message" /><button>JOIN US</button></div><p id="newsletter-message" aria-live="polite">{message || "Get updates about new colours, tricks, and offers."}</p></form>; }
