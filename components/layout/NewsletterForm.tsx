"use client";
import { FormEvent, useState } from "react";
export function NewsletterForm() { const [message, setMessage] = useState(""); const submit = (e: FormEvent<HTMLFormElement>) => { e.preventDefault(); const form = new FormData(e.currentTarget); const email = String(form.get("email") ?? ""); setMessage(/^\S+@\S+\.\S+$/.test(email) ? "Thanks — email updates will be connected soon." : "Please enter a valid email address."); }; return <form className="max-[900px]:col-span-full max-[560px]:col-auto" onSubmit={submit} noValidate><label className="mb-[15px] block text-[23px] font-[1000] text-logo-cyan" htmlFor="email">GET THE LATEST FIRST</label><div className="flex rounded-full bg-white p-[5px]"><input className="min-w-0 flex-1 border-0 bg-transparent p-3 text-ink outline-offset-4 focus-visible:outline-[3px] focus-visible:outline-white" id="email" name="email" type="email" placeholder="Enter your email" aria-describedby="newsletter-message" /><button className="rounded-full border-0 bg-logo-cyan p-[11px] text-[11px] font-[900] text-navy outline-offset-4 focus-visible:outline-[3px] focus-visible:outline-white">JOIN US</button></div><p className="text-xs text-[#b8c4cc]" id="newsletter-message" aria-live="polite">{message || "Get updates about new colours, tricks, and offers."}</p></form>; }
