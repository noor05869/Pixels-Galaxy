"use client";
import { FormEvent, useState } from "react";
export function NewsletterForm() { const [message, setMessage] = useState(""); const submit = (e: FormEvent<HTMLFormElement>) => { e.preventDefault(); const form = new FormData(e.currentTarget); const email = String(form.get("email") ?? ""); setMessage(/^\S+@\S+\.\S+$/.test(email) ? "Email note kar li — updates jald connect hongi." : "Sahi email address likhein."); }; return <form className="newsletter" onSubmit={submit} noValidate><label htmlFor="email">NAYI KHABAR SAB SE PEHLE</label><div><input id="email" name="email" type="email" placeholder="Apna email likhein" aria-describedby="newsletter-message" /><button>JOIN KAREIN</button></div><p id="newsletter-message" aria-live="polite">{message || "Naye colors, tricks aur offers ki khabar paayein."}</p></form>; }
