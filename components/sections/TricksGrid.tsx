import { Play } from "lucide-react";
import { trickTiles } from "@/lib/storefront/content";
import { MediaFrame } from "@/components/ui/MediaFrame";
import { Reveal } from "@/components/ui/Reveal";
export function TricksGrid() { return <section id="tricks" className="mx-auto mb-[100px] w-[min(calc(100%_-_48px),1320px)]"><div className="mb-7 flex items-end justify-between max-[560px]:block"><div><h2 className="m-0 text-[clamp(44px,6vw,80px)]">KU STRING IN MOTION</h2><p>See the product videos before choosing your colour.</p></div><a className="rounded-full border-2 border-white px-[18px] py-[13px] text-xs font-[900] outline-offset-4 focus-visible:outline-[3px] focus-visible:outline-white max-[560px]:mt-[15px] max-[560px]:inline-block" href="#featured">CHOOSE A COLOUR</a></div><div className="grid auto-rows-[260px] grid-cols-5 gap-4 max-[900px]:grid-cols-2 max-[560px]:auto-rows-[210px]">{trickTiles.map((tile,index) => <Reveal key={tile.id} delay={(index%5)*70} className={`${tile.size === "wide" ? "col-span-3 max-[900px]:col-span-2" : ""} min-w-0`}><article className="group relative h-full overflow-hidden rounded-[14px] after:absolute after:inset-0 after:bg-[linear-gradient(0deg,rgba(0,15,45,.75),transparent_60%)] after:content-['']"><MediaFrame className="absolute inset-0 transition-transform duration-[350ms] group-hover:scale-[1.04]" media={tile} /><video className="absolute inset-0 z-[1] size-full object-cover opacity-0 transition-opacity duration-200 group-hover:opacity-100 group-focus-within:opacity-100 motion-reduce:hidden" muted loop playsInline preload="metadata"><source src={`/videos/v${index%4+1}.mp4`} type="video/mp4" /></video><span className="absolute bottom-3.5 left-[15px] z-[3] text-lg font-[1000]">KU STRING VIDEO {String(index + 1).padStart(2, "0")}</span><i className="absolute right-3 top-3 z-[3] grid size-10 place-items-center rounded-full bg-[#1764d9] [&_svg]:w-[15px]"><Play fill="currentColor" /></i></article></Reveal>)}</div></section>; }
