import { siteContent } from "@/lib/storefront/content";
export function PressStrip() { const items = [...siteContent.press, ...siteContent.press]; return <section className="group w-full overflow-hidden bg-lime py-[25px] text-center text-navy max-[540px]:py-5 max-[540px]:text-xs" aria-label="As seen in"><div className="flex w-max animate-[press-marquee_28s_linear_infinite] items-center group-hover:[animation-play-state:paused] group-focus-within:[animation-play-state:paused] motion-reduce:animate-none motion-reduce:transform-none">{items.map((name, index) => <strong className="w-[260px] flex-[0_0_260px] text-center text-[clamp(12px,1.2vw,17px)] leading-none tracking-[.06em]" key={`${name}-${index}`} aria-hidden={index >= siteContent.press.length}>{name}</strong>)}</div></section>; }
